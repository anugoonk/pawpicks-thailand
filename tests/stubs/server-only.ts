// Test stub: `server-only` throws unless imported in an RSC context. Under
// vitest we exercise the pure helpers in these modules directly, so alias it
// to this no-op (see vitest.config.ts).
export {};
